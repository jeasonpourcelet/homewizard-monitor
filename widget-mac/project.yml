# XcodeGen spec — generates HomeWizardWidget.xcodeproj.
# Install XcodeGen (brew install xcodegen) then run `xcodegen generate` here.
name: HomeWizardWidget
options:
  bundleIdPrefix: io.github.homewizard-monitor
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true
settings:
  base:
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.0"
    CODE_SIGN_STYLE: Automatic
    # Stable signing identity (free Personal Team) — required so the placed
    # widget tile stays bound across rebuilds and actually refreshes.
    DEVELOPMENT_TEAM: J7LJJ65X8B

targets:
  HomeWizardWidget:
    type: application
    platform: macOS
    sources:
      - Sources/App
      - Sources/Shared
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.homewizard-monitor.widget
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: Home Wizard
        INFOPLIST_KEY_LSUIElement: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_ENTITLEMENTS: Sources/App/HomeWizardWidget.entitlements
    dependencies:
      - target: HWMWidgetExtension
        embed: true

  HWMWidgetExtension:
    type: app-extension
    platform: macOS
    sources:
      - Sources/Widget
      - Sources/Shared
    info:
      path: Sources/Widget/Info.plist
      properties:
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: io.github.homewizard-monitor.widget.ext
        INFOPLIST_KEY_CFBundleDisplayName: Home Wizard
        CODE_SIGN_ENTITLEMENTS: Sources/Widget/HWMWidget.entitlements
        SKIP_INSTALL: YES
